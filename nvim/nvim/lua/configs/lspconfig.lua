require("nvchad.configs.lspconfig").defaults()

-- Mason-lspconfig will automatically setup servers listed in plugins/init.lua
-- No need to manually configure them here - they're handled by mason-lspconfig.setup_handlers()

-- If you need custom configuration for a specific server, use mason-lspconfig handlers in plugins/init.lua
-- Or add custom configs here after Mason has set them up

-- read :h vim.lsp.config for changing options of lsp servers 
