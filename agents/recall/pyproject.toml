[project]
name = "agent-recall"
version = "0.1.0"
description = "Local semantic recall over Claude/Codex history files"
requires-python = ">=3.11"
dependencies = [
  "fastembed>=0.3.0",
  "numpy>=1.26",
]

[project.scripts]
agent-recall = "agent_recall.__main__:main"

